import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mecarreira.app',
  appName: 'mecarreira',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
